# JTAG boot via USB-Serial/JTAG nativo do ESP32-S3
source [find board/esp32s3-builtin.cfg]

proc init_script {} {}

init
halt
echo "=== JTAG connected and halted ==="
reset run
echo "=== reset run issued ==="
shutdown